import styled from "styled-components";

const Container = styled.div`
  display: flex;
  height: 50%;
  background-color:red;
`;
const Searchbar = () =>{
    return (
        <Container>

        </Container>
    )
}